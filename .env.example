# Default compatibility path: GitHub Models free tier.
# Create a fine-grained GitHub PAT with the `models:read` permission.
GITHUB_TOKEN=

# Optional legacy provider path; not used by the GitHub Models demo.
OPENAI_API_KEY=
