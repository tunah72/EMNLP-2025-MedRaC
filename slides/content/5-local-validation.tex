\section{Local Demo and Mini-Experiment}

\begin{frame}{Local Demo Objective and Pipeline}
  \centering
  \sectionlabel{Local demo pipeline}\par\vspace{0.16cm}
  \begin{tikzpicture}[
    node distance=0.24cm and 0.25cm,
    box/.style={draw=SeminarBlue,rounded corners=1.2pt,minimum height=0.62cm,align=center,fill=white,font=\scriptsize,inner xsep=4pt},
    arr/.style={-{Latex[length=2mm]},DeepBlue,thick}
  ]
    \node[box] (id) {Calculator ID};
    \node[box,below=of id] (note) {Patient Note};
    \node[box,below=of note] (question) {Question};
    \node[box,right=0.45cm of note] (retrieval) {Formula\\Retrieval};
    \node[box,right=of retrieval] (extract) {Value\\Extraction};
    \node[box,right=of extract] (code) {Code\\Generation};
    \node[box,right=of code] (execute) {Safe\\Execution};
    \node[box,below=0.48cm of execute] (answer) {Final Answer};
    \node[box,left=of answer] (evaluate) {Optional F--E--C--A\\Evaluation};
    \node[box,left=of evaluate] (ui) {Streamlit\\Inspection};
    \draw[arr] (id.east) -- ++(0.18,0) |- (retrieval.west);
    \draw[arr] (note.east) -- (retrieval.west);
    \draw[arr] (question.east) -- ++(0.18,0) |- (retrieval.west);
    \draw[arr] (retrieval) -- (extract);
    \draw[arr] (extract) -- (code);
    \draw[arr] (code) -- (execute);
    \draw[arr] (execute) -- (answer);
    \draw[arr] (answer) -- (evaluate);
    \draw[arr] (evaluate) -- (ui);
  \end{tikzpicture}
  \figcaption{Observable stages in the local seminar pipeline.}
  \vspace{0.16cm}

  \begin{columns}[T,onlytextwidth]
    \column{0.47\textwidth}
      \sectionlabel{Objective}\par
      Execute the workflow and inspect observable intermediate outputs.
    \column{0.49\textwidth}
      \sectionlabel{Boundary}\par
      The demo uses fixed benchmark rows, exposes structured outputs only, and applies seminar-specific safe execution.
  \end{columns}
\end{frame}

\begin{frame}{Streamlit Demo and Representative Example}
  \begin{columns}[T,onlytextwidth]
    \column{0.49\textwidth}
      \includegraphics[width=\linewidth,height=0.45\textheight,keepaspectratio]{streamlit_replay_question.png}
      \vspace{0.08cm}
      {\scriptsize Calculator ID, Patient Note, Question, and observable pipeline stages.}
    \column{0.49\textwidth}
      \includegraphics[width=\linewidth,height=0.45\textheight,keepaspectratio]{streamlit_replay_contract.png}
      \vspace{0.08cm}
      {\scriptsize Question-only retrieval, rank, similarity score, and retrieved Cockcroft-Gault formula.}
  \end{columns}
  \figcaption{Streamlit replay for Creatinine Clearance (Cockcroft-Gault Equation), Calculator ID 2.}
\end{frame}

\begin{frame}{Local Experiment 1: End-Task Evaluation}
  \centering
  \vspace{0.68cm}
  {\large
  \begin{tabular}{lrr}
    \toprule
    Method & Correct & Accuracy \\
    \midrule
    Plain CoT & 9/10 & $90\%$ \\
    MedRaC + RAG & \textbf{10/10} & \textbf{$100\%$} \\
    \bottomrule
  \end{tabular}}
  \tabcaption{Rule-based final-answer accuracy on ten fixed cases.}
  \vspace{0.62cm}

  {\small \bluekey{Observation:} MedRaC is correct on one additional case ($+10$ percentage points); the result is descriptive for $n=10$.}
\end{frame}

\begin{frame}{Local Experiment 2: Step-wise Evaluation}
  \centering
  \vspace{0.48cm}
  {\large
  \begin{tabular}{lrrrrr}
    \toprule
    Method & Formula & Extraction & Calculation & Answer & Strict \\
    \midrule
    Plain CoT & 10/10 & 9/10 & 10/10 & 8/10 & 8/10 \\
    MedRaC + RAG & 10/10 & \textbf{10/10} & 10/10 & \textbf{9/10} & \textbf{9/10} \\
    \bottomrule
  \end{tabular}}
  \tabcaption{Post-hoc F--E--C--A and strict correctness on stored outputs.}
  \vspace{0.58cm}

  {\small \bluekey{Observation:} MedRaC improves Extraction, Answer, and Strict by one case; Formula and Calculation remain unchanged.}
\end{frame}
